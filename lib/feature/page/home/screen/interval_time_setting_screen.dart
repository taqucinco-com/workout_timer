import 'package:flutter/material.dart';

class IntervalTimeSettingScreen extends StatelessWidget {
  const IntervalTimeSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Interval Time Setting'),
    );
  }
}
