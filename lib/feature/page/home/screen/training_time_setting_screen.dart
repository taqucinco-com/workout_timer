import 'package:flutter/material.dart';

class TrainingTimeSettingScreen extends StatelessWidget {
  const TrainingTimeSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Training Time Setting'),
    );
  }
}
